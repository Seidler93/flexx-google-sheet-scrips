{
  "name": "flexx-staff",
  "version": "0.1.0",
  "private": true,
  "description": "Flexx staff Google Apps Script web app.",
  "scripts": {
    "login": "clasp login",
    "login:bash": "clasp login --no-localhost",
    "pull": "clasp pull",
    "push": "clasp push",
    "push:force": "clasp push --force",
    "deploy:live": "node scripts/deploy-live.js",
    "backfill:ids": "clasp run backfillMemberIdsForAllLocations",
    "backfill:cancel-ids": "clasp run backfillCancellationMemberIdsForAllLocations",
    "setup:status-triggers": "clasp run setupMembershipStatusEditTriggers",
    "open": "clasp open-script",
    "status": "clasp status",
    "logs": "clasp logs"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.6"
  }
}
